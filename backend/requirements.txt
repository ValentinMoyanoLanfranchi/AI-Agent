# ─── FastAPI & ASGI ───────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9

# ─── Database ─────────────────────────────────────────────────
sqlalchemy[asyncio]==2.0.50
asyncpg==0.31.0
psycopg2-binary==2.9.12
alembic==1.13.1
geoalchemy2==0.20.0
greenlet==3.5.1

# ─── Redis & Celery ───────────────────────────────────────────
redis==5.0.4
celery==5.3.6
celery[redis]==5.3.6
kombu==5.3.4

# ─── LangGraph & LangChain ────────────────────────────────────
# Generación 0.3.x: la 0.2.x fija numpy<2, y numpy<2 no tiene wheel para
# Python 3.13 → 0.2.x es incompatible con 3.13. La 0.3.x admite numpy 2.
# langchain-community se removió: no se importa en el código.
langgraph>=0.2.40
langchain>=0.3,<0.4
langchain-openai>=0.2,<0.4
langchain-anthropic>=0.2,<0.4
langchain-core>=0.3,<0.4

# ─── HTTP & APIs ──────────────────────────────────────────────
httpx==0.27.0
aiohttp>=3.10.0          # 3.9.x no tiene wheel para Python 3.13
requests>=2.31.0        # resend 0.8.0 fija requests==2.31.0; dejamos que mande
tenacity==8.3.0

# ─── Data & Geo ───────────────────────────────────────────────
# Pisos >= : versiones viejas no tienen wheel cp313 y exigen compilar desde fuente.
numpy>=2.1.0             # primera serie con wheels para Python 3.13
pandas>=2.2.3
shapely>=2.0.6
pyproj>=3.7.0

# ─── Config & Utils ───────────────────────────────────────────
pydantic>=2.9            # 2.7.x / pydantic-core sin wheel cp313
pydantic-settings>=2.5
python-dotenv==1.0.1
python-jose[cryptography]==3.3.0

# ─── Microsoft Azure AI Foundry + Foundry IQ (Agents League) ──
# Si hay conflictos de versión, podés aflojar los pins.
azure-search-documents==11.5.1
openai==1.54.0

# ─── Notificaciones ───────────────────────────────────────────
resend==0.8.0

# ─── Testing ──────────────────────────────────────────────────
pytest==8.2.1
pytest-asyncio==0.23.7
httpx==0.27.0
